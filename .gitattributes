# Keep data files byte-identical across operating systems: prompts built from them are hashed
# into LLM cache keys, so a CRLF conversion on checkout would break offline reproduction.
*.csv -text
*.jsonl -text
